#format: sh
######################################################################
# 
# Configuration file for mgigff product. 
# ==> Most of mgigff's settings are in the file config.cfg !! This file is 
# basically a bridge to tie mgigff into mgiconfig's settings.
#

if [ "${MGICONFIG}" = "" ]
then
    MGICONFIG=/usr/local/mgi/live/mgiconfig
    export MGICONFIG
fi

. ${MGICONFIG}/master.config.sh


FTPURL=ftp.informatics.jax.org
export FTPURL

GNUSORT=/usr/local/bin/sort
export GNUSORT

